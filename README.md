# Mini Project 3 – Build Your Own Self-Hosted CI Runner

## 🎯 Project Goal

Create your own self-hosted GitHub Actions runner on a Linux machine.

Your final architecture should be:

```text
Developer
    |
    | git push
    v
GitHub Repository
    |
    v
GitHub Actions
    |
    | runs-on: self-hosted
    v
Your Linux Machine
    |
    +── Docker
    +── Python
    +── Git
    +── Shell
```

This project extends the Day-42 runner tasks into a small real-world DevOps environment.

---

# 🧠 What You Will Learn

You will understand:

- GitHub-hosted runners
- Self-hosted runners
- Runner registration
- Runner labels
- Runner services
- `runs-on`
- Runner security
- Workspace
- Environment differences
- Persistent machines
- CI jobs running on your own infrastructure

---

# 📁 Project Structure

```text
self-hosted-ci/
├── .github/
│   └── workflows/
│       └── self-hosted.yml
│
├── scripts/
│   ├── system-info.sh
│   └── deploy.sh
│
├── app/
│   └── app.py
│
└── README.md
```

---

# Task 1 – Understand GitHub-Hosted Runner

First run a workflow using:

```yaml
runs-on: ubuntu-latest
```

Print:

```bash
hostname
whoami
pwd
python --version
docker --version
git --version
```

Understand:

```text
GitHub
   |
   +── Creates runner
   |
   +── Runs job
   |
   +── Executes workflow
   |
   +── Removes/refreshes environment
```

---

# Task 2 – Prepare Your Linux Machine

Use:

- Your Linux computer
- A VM
- A cloud VM

Check:

```bash
hostname
whoami
pwd
python3 --version
git --version
docker --version
```

Record the output.

---

# Task 3 – Register Self-Hosted Runner

Go to:

```text
GitHub Repository
      ↓
Settings
      ↓
Actions
      ↓
Runners
      ↓
New self-hosted runner
```

Choose:

```text
Linux
x64
```

Follow the commands generated by GitHub.

Do not manually invent the registration token.

---

# Task 4 – Start Runner

Start the runner according to GitHub's generated instructions.

You should eventually see:

```text
Connected to GitHub

Listening for Jobs
```

On GitHub:

```text
Self-hosted runner
        🟢 Idle
```

---

# Task 5 – Create Workflow

Create:

```text
.github/workflows/self-hosted.yml
```

Use:

```yaml
runs-on: self-hosted
```

The workflow should print:

```text
Hostname
Username
Working directory
Operating system
Python version
Git version
Docker version
```

---

# Task 6 – Verify It Is Your Machine

This is the important part.

Run:

```bash
hostname
```

on your machine.

Then compare it with the GitHub Actions output.

They should match.

You should be able to say:

> "This GitHub Actions job actually executed on my machine."

---

# Task 7 – Create a File

From GitHub Actions, create:

```text
runner-test.txt
```

Example contents:

```text
Created by GitHub Actions
```

Then check your Linux machine:

```bash
ls
```

and:

```bash
cat runner-test.txt
```

---

# Task 8 – Add a Runner Label

Give your runner a custom label such as:

```text
my-linux-runner
```

Now configure your workflow to target:

```text
self-hosted
+
my-linux-runner
```

Conceptually:

```text
GitHub Actions
      |
      | labels
      v
self-hosted
      +
my-linux-runner
      |
      v
Your Linux machine
```

---

# Task 9 – Build a Small Deployment Script

Create:

```text
scripts/deploy.sh
```

The script should:

1. Print hostname
2. Print current user
3. Print deployment time
4. Create a deployment directory
5. Copy application files
6. Print deployment status

Example architecture:

```text
Git Push
   |
   v
GitHub Actions
   |
   v
Self-Hosted Runner
   |
   v
deploy.sh
   |
   v
Application
```

---

# Task 10 – Docker Challenge

Since this is a DevOps project, check whether Docker is available on your runner:

```bash
docker --version
```

Then create a workflow step that runs:

```bash
docker ps
```

Understand why Docker availability on your own runner can be useful.

---

# Task 11 – Compare Runners

Create this table in your notes:

| Feature | GitHub-Hosted | Self-Hosted |
|---|---|---|
| Machine managed by | ? | ? |
| Infrastructure | ? | ? |
| Software control | ? | ? |
| Maintenance | ? | ? |
| Customization | ? | ? |
| Security responsibility | ? | ? |
| Best use case | ? | ? |
| Persistent environment | ? | ? |

Fill it using your own understanding.

---

# Task 12 – Stop the Runner

Stop the runner.

Then trigger the workflow again.

Observe what happens.

Ask yourself:

```text
Why is the job waiting?
```

Start the runner again.

Observe:

```text
Waiting
   ↓
Runner available
   ↓
Job starts
```

This is a very important concept.

---

# 🔥 Advanced Challenge – Runner Labels

Create two labels:

```text
self-hosted
linux
my-linux-runner
docker
```

Then configure workflows to target different labels.

For example:

```text
self-hosted + docker
```

should only execute on a runner that has Docker capability.

---

# 🔥 Advanced Challenge – Deployment Pipeline

Create:

```text
CI
 ↓
Test
 ↓
Build
 ↓
Deploy
```

The final deployment job must run on your self-hosted runner.

Architecture:

```text
                 Git Push
                    |
                    v
              GitHub Actions
                    |
          +---------+---------+
          |                   |
          v                   v
       Test Job           Build Job
          |                   |
          +---------+---------+
                    |
                    v
             Deploy Job
                    |
                    v
          Self-Hosted Runner
                    |
                    v
              Your Machine
```

---

# ❓ Questions You Must Answer

### 1. What is a runner?

### 2. Who manages GitHub-hosted runners?

### 3. Who manages self-hosted runners?

### 4. What does `runs-on` do?

### 5. Why use self-hosted runners?

### 6. What are the security risks?

### 7. Why are labels useful?

### 8. What happens when the runner is offline?

### 9. Why can self-hosted runners be useful for Docker deployments?

### 10. What happens to files created by a self-hosted runner?

---

# 🚨 Security Challenge

Think about this carefully.

A GitHub Actions job can execute shell commands.

Therefore:

```yaml
run: some-command
```

is executing commands on your machine when using a self-hosted runner.

Ask yourself:

> What could happen if an untrusted Pull Request executes a malicious command?

Research:

- Untrusted code
- Pull Request security
- Secrets
- Runner isolation
- Runner cleanup
- Ephemeral runners

Do not expose sensitive credentials on your runner.

---

# ✅ Expected Output

GitHub should show:

```text
Settings
   ↓
Actions
   ↓
Runners
   ↓
🟢 my-linux-runner
```

And the workflow should show:

```text
Self Hosted Job
      ✅
```

The job output should contain your machine's:

```text
Hostname
Username
Python
Git
Docker
Working Directory
```

---

# 🎓 What You Should Understand

After completing this project, you should be able to explain:

> "A runner is the machine that executes a GitHub Actions job. GitHub-hosted runners are managed by GitHub, while self-hosted runners are machines that I manage and register with GitHub."

You should also understand why:

```yaml
runs-on: self-hosted
```

is fundamentally different from:

```yaml
runs-on: ubuntu-latest
```

---

# 🚀 Final Challenge

Build this complete pipeline:

```text
Developer
    |
    | git push
    v
GitHub
    |
    v
CI Workflow
    |
    +───────────────+
    |               |
    v               v
Unit Tests        Lint
    |               |
    +───────+───────+
            |
            v
          Build
            |
            v
       Self-Hosted
          Runner
            |
            v
        Docker Build
            |
            v
        Application
```

If you can build and explain this pipeline yourself, you have moved beyond simply following GitHub Actions tutorials.