name: Scheduled Trigger

on:
  workflow_dispatch:

  # push:
  #   branches:
  #     - main
  # schedule:
  #   - cron: "* 0 * * *"

jobs:
  update:
    runs-on: self-hosted
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v6

      - name: Scheduled
        run: echo "every midnight update the update the dependency"
